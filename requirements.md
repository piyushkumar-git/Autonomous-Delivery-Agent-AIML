# Requirements

## Software
- Python >= 3.8

## Dependencies
- Standard library only (argparse, json, heapq, random, math, time)
- Optional: matplotlib

Install matplotlib with:
pip install matplotlib